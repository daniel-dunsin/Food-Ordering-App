const { default: styled } = require("styled-components");

const NoItemStyled = styled.section`
    .no-item{
        text-align: center;
        padding: 4rem 1rem 6rem;
    }
`

export default NoItemStyled;